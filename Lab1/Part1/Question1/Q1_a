%Julian Aloise
%40177178
%Question1a

clear 
clc

%declare inputs
n = [0:9];

%declare the signals
x = n; %input signal
y = x.^2; %output signal

%plot input signal
subplot(1,2,1)
stem(n,x)
xlabel('n')
ylabel('x[n]') title('Input Signal x[n]')

%plot output signal
subplot(1,2,2)
stem(n,y)
xlabel('n')
ylabel('y[n]') title('Output Signal y[n]')

%initializing energy variables to zero
total_energy_x = 0;
total_energy_y = 0;

%compute the total energy for the input and output signal
for i = 1:10
    total_energy_x =
    total_energy_y =
end

%Display energy
disp('The energy for disp(total_energy_x) disp('The energy for disp(total_energy_y)
total_energy_x + x(i).^2; total_energy_y + y(i).^2;
input signal x is ')
output signal y is ')
